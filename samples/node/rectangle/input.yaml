# rectangle — the bread-and-butter iso box: three faces, top label, extrusion h.
canvas:
  background: "#FAFBFC"

nodes:
  scene:
    shape: rectangle
    label: "API"
    geom: { w: 140, d: 100, h: 32 }
