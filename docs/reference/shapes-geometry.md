# Shape Geometry Reference

iso-topology ships with a growing library of 3D shape families beyond the
classic box. Each family has a **shape provider** that computes projected
face polygons from a simple set of parameters; all providers share the same
surface-material vocabulary (`style.palette`, `style.faces`, gradients,
patterns, effects).

---

## Shape families at a glance

| Family | Built-in shapes | Key parameter |
|--------|----------------|---------------|
| **Box** | `rectangle`, `square` | — |
| **Prism** | `prism`, `diamond`, `triprism`, `hexprism`, `octprism` | `geom.sides` |
| **Tapered prism** | `cone`, `pyramid`, `frustum` | `geom.topScale`, `geom.sides` |
| **Revolution body** | `dome`, `torus`, `capsule` | — |
| **Cylinder** | `cylinder`, `queue`, `stored_data` | — |
| **Sphere** | `circle` | — |
| **Cloud** | `cloud` | — |

---

## Prism family (`shape: prism`)

Any regular n-gon extruded vertically. The `diamond`, `triprism`,
`hexprism`, `octprism` aliases fix the side count; bare `prism` reads
`geom.sides`.

```yaml
nodes:
  gw:
    shape: hexprism
    geom: { w: 150, d: 150, h: 44 }
    label: "API Gateway"
    style:
      palette: { top: "#EDE9FE", left: "#6D28D9", right: "#8B5CF6" }
```

| Alias | Sides | Semantic use |
|-------|-------|-------------|
| `diamond` | 4 | Decision node, routing |
| `triprism` | 3 | Alert, one-way dispatch |
| `hexprism` | 6 | API gateway, middleware |
| `octprism` | 8 | Firewall (stop-sign) |
| `prism` + `geom.sides: N` | N | Any regular polygon |

---

## Tapered prism family (`cone`, `pyramid`, `frustum`)

Regular n-gon base whose top face is uniformly scaled toward the centroid.

```
topScale = 0    → apex (cone / pyramid)
topScale ∈ (0,1) → frustum / truncated cone
topScale = 1    → regular prism (same as prism family)
```

### Parameters

| Field | Default | Description |
|-------|---------|-------------|
| `geom.sides` | cone=32, pyramid=4, frustum=32 | Number of base polygon sides |
| `geom.topScale` | cone/pyramid=0, frustum=0.5 | Top-face scale factor (0–1) |

### Examples

**Cone** (smooth, 32-sided approximation):
```yaml
nodes:
  traffic:
    shape: cone
    geom: { w: 140, d: 140, h: 100 }
    label: "Load Balancer"
    style:
      palette: { top: "#FDE68A", left: "#D97706", right: "#F59E0B" }
```

**Pyramid** (square base, `topScale=0`):
```yaml
nodes:
  hierarchy:
    shape: pyramid
    geom: { w: 140, d: 140, h: 100 }
    label: "Data Pyramid"
    style:
      palette: { top: "#E0E7FF", left: "#3730A3", right: "#4F46E5" }
```

**Hexagonal pyramid** (custom sides):
```yaml
nodes:
  funnel:
    shape: cone          # or pyramid
    geom: { w: 140, d: 140, h: 90, sides: 6 }
    label: "Funnel"
    style:
      palette: { top: "#FEF3C7", left: "#B45309", right: "#D97706" }
```

**Frustum** (truncated cone — S3-style storage bucket):
```yaml
nodes:
  bucket:
    shape: frustum
    geom: { w: 140, d: 140, h: 90 }     # topScale defaults to 0.5
    label: "Object Storage"
    style:
      palette: { top: "#D1FAE5", left: "#065F46", right: "#059669" }
```

**Partially truncated frustum** (custom topScale):
```yaml
nodes:
  near-apex:
    shape: frustum
    geom: { w: 140, d: 140, h: 90, topScale: 0.15 }
    label: "Funnel\nEgress"
    style:
      palette: { top: "#FFEDD5", left: "#C2410C", right: "#EA580C" }
```

### Semantic guide

| Shape | `topScale` | `sides` | Typical use |
|-------|-----------|---------|------------|
| `cone` | 0 | 32 | Traffic convergence, load balancer |
| `pyramid` | 0 | 4 | Data pyramid, layered architecture |
| `frustum` | 0.5 | 32 | Object storage (S3/GCS bucket) |
| `frustum` | 0.3 | 6 | Hexagonal hopper, distribution funnel |

---

## Revolution body family (`dome`, `torus`, `capsule`)

Smooth shapes generated by revolving a profile curve around the Z-axis.
The iso projection renders the visible front arc as a polygon strip.

### `dome`

Quarter-circle profile — a hemispherical cap. Useful for security zones,
isolated regions, protective enclosures.

```yaml
nodes:
  zone:
    shape: dome
    geom: { w: 150, d: 150, h: 80 }
    label: "Security Zone"
    style:
      palette: { top: "#DBEAFE", left: "#1E40AF", right: "#2563EB" }
      effects:
        backglow: { color: "#3B82F6", radius: 32, opacity: 0.4 }
```

### `torus`

Donut shape. Useful for representing circular topologies: consistent
hashing rings, replication rings, circular queues.

```yaml
nodes:
  ring:
    shape: torus
    geom: { w: 160, d: 160, h: 80 }
    label: "Hash Ring"
    style:
      palette: { top: "#FDF4FF", left: "#7E22CE", right: "#9333EA" }
```

### `capsule`

Cylinder with hemispherical end caps. Useful for containers, pods,
process instances.

```yaml
nodes:
  instance:
    shape: capsule
    geom: { w: 120, d: 120, h: 120 }
    label: "Pod"
    icon: "iso://glyph/box"
    style:
      palette: { top: "#F0FDF4", left: "#15803D", right: "#16A34A" }
```

---

## Effect pipeline (`style.effects.effects`)

Effects can now be expressed as an **ordered list** in addition to the
individual scalar fields. When the list is present it takes precedence.

```yaml
style:
  effects:
    effects:
      - kind: backglow
        color: "#A78BFA"
        radius: 40
        opacity: 0.5
      - kind: grain
        intensity: 0.2
        scale: 1.0
      - kind: outline
        color: "#DDD6FE"
        width: 2.5
        opacity: 0.9
```

### Supported effect kinds

| Kind | Parameters | Description |
|------|-----------|-------------|
| `backglow` | `color`, `radius`, `opacity` | Diffuse halo behind the shape |
| `blur` | `stdDev` (or `blur`) | Gaussian blur — ghost/fog nodes |
| `grain` | `intensity`, `scale` | Film-grain noise overlay |
| `outline` | `color`, `width`, `dash`, `opacity` | Silhouette accent ring |
| `dropShadow` | `color`, `dx`, `dy`, `blur` | Drop shadow under the shape |

The same kind may appear multiple times in the list; each entry is applied
independently in order. This allows, for example, a tight inner glow
followed by a wide outer glow by listing `backglow` twice with different
radii.

### Priority rules

```
style.effects.effects (list)  overrides  style.effects.backglow (scalar)
style.effects.effects (list)  overrides  style.effects.outline (scalar)
... and so on for every effect kind.
```

Scalar fields (the old API) remain fully supported and are unaffected when
`effects` list is absent.

---

## Surface vocabulary

All shape families support the same `style.faces` overrides for per-face
fills, multi-stop gradients, and multi-layer strokes. Face names depend on
the shape:

| Shape family | Face names |
|-------------|-----------|
| Box | `top`, `left`, `right` |
| Prism | `top`, `side0` … `sideN-1` |
| Tapered prism | `top` (frustum only), `side0` … `sideN-1` |
| Revolution body | `top` (when radius > 0), `band0` … `bandM-1` |
| Cylinder | `top`, `body`, `bottom-cap` |

```yaml
style:
  faces:
    top:
      fill:
        kind: linearGradient
        stops:
          - { offset: 0, color: "#A78BFA" }
          - { offset: 1, color: "#6D28D9" }
        angle: 135
      stroke:
        layers:
          - { color: "#DDD6FE", width: 1.5, offset: -1 }   # inner highlight
    side0:
      fill: { kind: solid, color: "#4338CA" }
```

See [DSL YAML reference](./dsl-yaml.md) for the full `style.faces` schema.
